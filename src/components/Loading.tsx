import React from "react";

const Loading: React.FC = () => {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <h1 className="text-2xl animate-pulse">Ready?</h1>
    </div>
  );
};

export default Loading;
